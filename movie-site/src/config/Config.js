const config = {
    movieApiUrl: String(import.meta.env.VITE_MOVIE_API_URL),
    movieApiKey: String(import.meta.env.VITE_API_KEY),
    moviePosterUrl:String(import.meta.env.VITE_POSTER_URL)
}

export default config;